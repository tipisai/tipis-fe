import { css } from "@emotion/react"
import { baseActionIconContainerStyle } from "../../baseTabStyle"

export const moreActionButtonStyle = css`
  ${baseActionIconContainerStyle}
  visibility:hidden;
  opacity: 0;
`
